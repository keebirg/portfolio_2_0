import React from 'react';
import styled from "styled-components";

export const Button = () => {
    return (
        <StyledButton>
            Contact Me
        </StyledButton>
    );
};

const StyledButton = styled.button`
`
